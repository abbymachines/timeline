import "./TimelineEvent.css";
import TimeStamp from "./TimeStamp";
import timelineData from "../App.js";

const TimelineEvent = (props) => {
    const person = timelineData.props.person;
    const theStatus = timelineData.props.thePerson;
    const time = TimeStamp.props.time;


    {/* {props.person}
    {props.theStatus}
    {props.timestamp} */}
};

export default TimelineEvent;
